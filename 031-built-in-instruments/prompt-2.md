Copy the prompt below and paste it into Claude, ChatGPT, or your model of choice. The article linked from this directory's README explains what to expect.

````text
You are an aggregator script writer. You operate read-only against my live AI configuration. You may write only inside the scratch directory used by Prompt 1. You may not exfiltrate file contents. You may not read `*.env`, `*credentials*`, `*token*`, `*.key`, `*.pem`, `id_rsa*`, `id_ed25519*`, `/.ssh/`, or `/.gnupg/` paths. You may not modify `~/.claude/settings.json`, `~/CLAUDE.md`, `~/.claude/CLAUDE.md`, any memory file, any hook, or install MCP servers. You may not write to `~/bin`, `~/.bashrc`, `~/.zshrc`, or `/usr/local/bin`. The output script is staged; the human chooses where (or whether) to install it.

**Step 1 — locate Prompt 1 output.** Run:

```bash
SCRATCH="$HOME/.claude/experiments/$(date +%Y-%m-%d)-031-instruments"
test -f "$SCRATCH/00-INSTRUMENTS.md" || { echo "ERROR: run prompt-1 first"; exit 1; }
```

Read `00-INSTRUMENTS.md`. Identify the subset where `available=Y` and `value_signal` is one of: `cost`, `context-shape`, `security-surface`, `health`. Write the chosen subset to `$SCRATCH/30-subset.md`.

**Step 2 — design the aggregator.** For each chosen instrument, define one read-only invocation that produces a single-line summary (≤80 chars). Examples:

- context-shape: word count of memory files → `MEM=$(cat ~/CLAUDE.md ~/.claude/CLAUDE.md 2>/dev/null | wc -w)`
- security-surface: allow-rule count → `ALLOW=$(jq '.permissions.allow | length' ~/.claude/settings.json)`
- health: hook state file presence → `HOOKS=$(ls ~/.claude/.engram-* 2>/dev/null | wc -l)`
- cost: latest transcript size in MB → `TR=$(du -sm ~/.claude/projects/-home-psimmons/<latest> | cut -f1)`

Write the design to `$SCRATCH/31-design.md`.

**Step 3 — produce the script.** Write `$SCRATCH/32-ai-status.sh` containing:

```bash
#!/usr/bin/env bash
# ai-status — aggregated read-only snapshot of AI surface state
# Generated by Article 031 Prompt 2 on <DATE>
set -uo pipefail

# <one read-only line per chosen instrument>
# echo "key=value" pairs only

# final compact summary
```

Constraints: bash only; no `curl`/`xh` to external endpoints; no writes; no `sudo`; no `eval`; no command substitution that reads files in the deny-list. Each line must be guarded with `2>/dev/null` so missing instruments degrade silently with a `key=N/A` value rather than failing the script.

Make the script executable: `chmod +x "$SCRATCH/32-ai-status.sh"`.

**Step 4 — run the script and capture output.** Execute `bash "$SCRATCH/32-ai-status.sh" > "$SCRATCH/33-sample-output.txt"`. Verify exit code 0. If non-zero, diagnose and fix in scratch only.

**Step 5 — write the install note.** Write `$SCRATCH/30-INSTALL.md` describing how to install the script if the operator chooses: copy to `~/bin/ai-status`, add `~/bin` to PATH if absent, run `ai-status` whenever wanted. Explicitly note: **the prompt does not perform the install**. The operator decides.

**Step 6 — write the plan summary.** Produce `$SCRATCH/30-PLAN.md` with: chosen instruments, sample output, install command (single `cp` line), uninstall command (single `rm` line), and a note that the script is read-only by construction. Print to stdout.
````
