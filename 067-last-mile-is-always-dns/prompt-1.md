Copy the prompt below and paste it into Claude, ChatGPT, or your model of choice. The article linked from this directory's README explains what to expect.

````text

Before marking the integration done, answer from production-equivalent context: **(1)** what segment does the agent run from, **(2)** what exact host:port does it call, **(3)** does that name resolve from that segment, **(4)** is that egress explicitly allowed? Any unknown = failed deploy check.

FIXTURE:
*(describe your integration: where the agent or service runs, what host:port it calls, and what you know about DNS resolution and egress policy from the runtime segment — not from your laptop)*
````
